<main class="container mt-5">
  <div class="row">
    <div class="col-12">
      <h1 class="mb-4 text-center" style="color: red;"><strong>Parties</strong></h1>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="col-12">
      <h2 class="mb-4 text-center" style="color: blue;"><strong>Create a Party</strong></h2>
      <form id="create-party-form">
        <div class="mb-3">
        <div class="text-center">
          <label for="party-name" class="form-label" style="color: red;">Party Name</label>
        </div>
          <input type="text" class="form-control" id="party-name" name="party-name" required>
        </div>
        {{!-- <div class="mb-3">
          <label for="party-date" class="form-label">Party Date</label>
          <input type="date" class="form-control" id="party-date" name="party-date" required>
        </div>
        <div class="mb-3">
          <label for="party-time" class="form-label">Party Time</label>
          <input type="time" class="form-control" id="party-time" name="party-time" required>
        </div>
        <div class="mb-3">
          <label for="party-location" class="form-label">Party Location</label>
          <input type="text" class="form-control" id="party-location" name="party-location" required>
        </div> --}}
        <div class="text-center">
          <button type="submit" class="btn btn-warning btn-lg mx-auto">Create Party</button>
        </div>
      </form>
      </form>
    </div>

    <div class="col-12">
      <h2 class="mb-4 text-center" style="color: blue;"><strong>Your Parties</strong></h2>
      <div class="list-group bg-info" id="party-list">
        {{#each parties}}
          <a href="/party-detail/{{this.id}}" class="list-group-item list-group-item-action">
            <h5 class="mb-1" style="color: red;"><strong>{{this.name}}</strong></h5>
            {{!-- <p class="mb-1">Date: {{date}}</p>
            <p class="mb-1">Time: {{time}}</p>
            <p class="mb-1">Location: {{location}}</p> --}}
          </a>
          {{/each}}
      </div>
    </div>
  </div>
</main>
